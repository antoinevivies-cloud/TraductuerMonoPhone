workflows:
  swift-playgrounds-validation:
    name: Swift Playgrounds package validation
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      xcode: latest
    scripts:
      - name: Validate the package manifest
        script: |
          swift package describe
          swift package dump-package
